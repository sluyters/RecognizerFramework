{
  "name": "g-handler",
  "version": "1.0.0",
  "description": "An interface that connects to a gesture recogniser and allows to specify handlers for each kind gesture.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "websocket": "^1.0.31"
  }
}
